{{title 'Fases del Torneo'}}
{{#paper-toolbar as |toolbar|}}
  {{#paper-toolbar-tools class="no-padding"}}
    {{#paper-button noInk=true class="no-padding" onClick=(transition-to 'scoreboard.groups')}}
      Grupos
    {{/paper-button}}

    {{#paper-button noInk=true class="no-padding" onClick=(transition-to 'scoreboard.quarters')}}
      Cuartos
    {{/paper-button}}

    {{#paper-button noInk=true class="no-padding" onClick=(transition-to 'scoreboard.semifinals')}}
      Semifinal
    {{/paper-button}}
    {{#paper-button noInk=true class="no-padding" onClick=(transition-to 'scoreboard.finals')}}
      Final
    {{/paper-button}}

  {{/paper-toolbar-tools}}
{{/paper-toolbar}}

{{outlet}}