{{title "Calendario"}}
{{#paper-toolbar as |toolbar|}}
  {{#paper-toolbar-tools class="no-padding"}}
    {{#paper-button noInk=true class="no-padding" onClick=(transition-to 'calendar.saturday')}}
      Sabado 7
    {{/paper-button}}

    {{#paper-button noInk=true class="no-padding" onClick=(transition-to 'calendar.sunday')}}
      Domingo 8
    {{/paper-button}}

    {{#paper-button noInk=true class="no-padding" onClick=(transition-to 'calendar.monday')}}
      Lunes 9
    {{/paper-button}}
  {{/paper-toolbar-tools}}
{{/paper-toolbar}}
{{outlet}}
